import { Dimensions } from "react-native";
import { primary, primaryDark, primaryLight } from "../../styles/colors";
export const styles = {
  containerDark: {
    backgroundColor: primaryDark,
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  containerLight: {
    backgroundColor: primaryLight,
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  img: {
    marginVertical: 50,
  },
};
